services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "8088:8088"
    environment:
      OCHAG_CODE: ${OCHAG_CODE}
      OCHAG_DB: /app/data/ochag.sqlite
      TZ: Europe/Moscow
