# Скопируйте в .env и заполните перед `docker compose up`.
# Семейный код доступа (обязателен; контейнер без него не стартует — ТЗ §7.6).
OCHAG_CODE=придумайте-общую-кодовую-фразу
# Домен для автоматического HTTPS через Caddy (ТЗ §10).
OCHAG_DOMAIN=ochag.example.com
